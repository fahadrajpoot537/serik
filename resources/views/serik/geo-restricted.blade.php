<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <title>Serik Realty — Availability</title>
    <style>
        :root {
            --bg: #f7f8fa;
            --text: #161e2d;
            --muted: #5b6573;
            --card: #ffffff;
            --accent: #0255a1;
            --border: rgba(22, 30, 45, 0.08);
        }
        * { box-sizing: border-box; }
        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 24px;
            font-family: Georgia, "Times New Roman", serif;
            background:
                radial-gradient(1200px 600px at 10% -10%, rgba(2, 85, 161, 0.12), transparent 55%),
                radial-gradient(900px 500px at 100% 0%, rgba(22, 30, 45, 0.08), transparent 50%),
                var(--bg);
            color: var(--text);
        }
        .card {
            width: 100%;
            max-width: 520px;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 18px;
            padding: 2rem 1.75rem;
            box-shadow: 0 1px 2px rgba(16, 24, 40, 0.04), 0 16px 40px rgba(16, 24, 40, 0.08);
            text-align: center;
        }
        .brand {
            font-size: 0.8rem;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            color: var(--accent);
            font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
            font-weight: 700;
            margin-bottom: 1rem;
        }
        h1 {
            margin: 0 0 0.85rem;
            font-size: clamp(1.35rem, 4vw, 1.75rem);
            line-height: 1.25;
            font-weight: 700;
        }
        p {
            margin: 0;
            font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
            font-size: 1rem;
            line-height: 1.6;
            color: var(--muted);
        }
        .regions {
            margin-top: 1.25rem;
            padding-top: 1.15rem;
            border-top: 1px solid var(--border);
            font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
            font-size: 0.9rem;
            color: var(--text);
            font-weight: 600;
        }
    </style>
</head>
<body>
    <main class="card" role="main">
        <div class="brand">Serik Realty</div>
        <h1>This website is currently available only in Canada, the United States, and Pakistan.</h1>
        <p>We’re focused on serving visitors in these regions right now. If you believe this is a mistake, please try again from a supported location.</p>
        <div class="regions">Canada · United States · Pakistan</div>
    </main>
</body>
</html>
